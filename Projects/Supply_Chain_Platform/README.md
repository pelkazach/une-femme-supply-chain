# Une Femme Supply Chain Platform

Supply chain intelligence API for inventory tracking and forecasting.

## Endpoints

- `GET /health` - Health check
- `GET /metrics` - Inventory metrics  
- `GET /inventory/sellable` - Current inventory
- `POST /upload` - Upload distributor files
